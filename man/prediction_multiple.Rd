% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_multiple}
\alias{prediction_multiple}
\title{Batch prediction for m6A}
\usage{
prediction_multiple(ml_fit, feature_df, positive_threshold = 0.5)
}
\arguments{
\item{ml_fit}{A trained classification model that supports
\code{predict(..., type = "prob")}, such as a tidymodels or caret model,
with a \code{"Positive"} probability column.}

\item{feature_df}{A data frame containing features of RNA sites to predict.
Required columns:
\itemize{
\item \code{gc_content} – numeric GC content (0–1)
\item \code{RNA_type} – one of \code{"mRNA"}, \code{"lincRNA"}, \code{"lncRNA"}, \code{"pseudogene"}
\item \code{RNA_region} – one of \code{"CDS"}, \code{"intron"}, \code{"3'UTR"}, \code{"5'UTR"}
\item \code{exon_length} – numeric exon length
\item \code{distance_to_junction} – numeric distance to exon–intron boundary
\item \code{evolutionary_conservation} – numeric conservation score
\item \code{DNA_5mer} – a string of 5 nucleotides (e.g., \code{"ATGCG"})
}}

\item{positive_threshold}{Probability threshold for assigning \code{"Positive"} status.
Default is \code{0.5}.}
}
\value{
A data frame containing the original features plus:
\itemize{
\item \code{predicted_m6A_prob} – predicted probability (numeric)
\item \code{predicted_m6A_status} – \code{"Positive"} or \code{"Negative"} (character)
}
}
\description{
This function predicts m6A methylation probability and binary status
for a batch of RNA sites using a trained classification model.
It validates the input column structure, encodes categorical variables,
performs one-hot encoding for 5-mer sequences, and returns prediction results.
}
\examples{
model <- readRDS(system.file("extdata", "rf_fit.rds", package = "m6APrediction"))
input_df <- read.csv(system.file("extdata", "m6A_input_example.csv", package = "m6APrediction"))
prediction_multiple(model, input_df, positive_threshold = 0.6)

}
